@{
    // Use different layouts based on user role
    if (User.Identity.IsAuthenticated && User.IsInRole("User"))
    {
        // Simplified layout for regular users
        Layout = "_LayoutUser";
    }
    else
    {
        // Default layout for admins and other roles
        Layout = "_Layout";
    }
}