@{
    ViewData["Title"] = "Integrations";
}

<div class="page-header">
    <div>
        <h1 class="page-title">Integrations</h1>
        <p class="text-muted">Connect your AI chatbots with external systems and services</p>
    </div>
    <div class="page-actions">
        <button class="btn btn-primary" id="refreshIntegrations">
            <i class="bi bi-arrow-repeat"></i>
            <span>Refresh</span>
        </button>
    </div>
</div>

<div class="integrations-container">
    <!-- Available Integrations Section -->
    <div class="section-header">
        <h2>Available Integrations</h2>
        <p class="section-description">Connect your AI chatbots with these popular services</p>
    </div>
    
    <div class="integrations-grid">
        <!-- CRM Integration -->
        <div class="integration-card">
            <div class="integration-header">
                <div class="integration-logo crm">
                    <i class="bi bi-people"></i>
                </div>
                <div class="integration-status">Coming Soon</div>
            </div>
            <div class="integration-body">
                <h3 class="integration-title">CRM Systems</h3>
                <p class="integration-description">Connect with popular CRM platforms like Salesforce, HubSpot, and Zoho to access customer data and update records.</p>
                <div class="integration-platforms">
                    <span class="platform-badge">Salesforce</span>
                    <span class="platform-badge">HubSpot</span>
                    <span class="platform-badge">Zoho</span>
                </div>
            </div>
            <div class="integration-footer">
                <button class="btn btn-sm btn-outline" disabled>
                    <i class="bi bi-plus-lg"></i>
                    Connect
                </button>
            </div>
        </div>
        
        <!-- Ticketing Systems -->
        <div class="integration-card">
            <div class="integration-header">
                <div class="integration-logo ticketing">
                    <i class="bi bi-ticket-perforated"></i>
                </div>
                <div class="integration-status">Coming Soon</div>
            </div>
            <div class="integration-body">
                <h3 class="integration-title">Ticketing Systems</h3>
                <p class="integration-description">Integrate with ticketing platforms to create, update, and resolve support tickets automatically.</p>
                <div class="integration-platforms">
                    <span class="platform-badge">Zendesk</span>
                    <span class="platform-badge">Freshdesk</span>
                    <span class="platform-badge">ServiceNow</span>
                </div>
            </div>
            <div class="integration-footer">
                <button class="btn btn-sm btn-outline" disabled>
                    <i class="bi bi-plus-lg"></i>
                    Connect
                </button>
            </div>
        </div>
        
        <!-- Knowledge Base -->
        <div class="integration-card">
            <div class="integration-header">
                <div class="integration-logo knowledge">
                    <i class="bi bi-book"></i>
                </div>
                <div class="integration-status">Coming Soon</div>
            </div>
            <div class="integration-body">
                <h3 class="integration-title">Knowledge Bases</h3>
                <p class="integration-description">Connect to external knowledge bases and documentation systems to enhance your chatbot's responses.</p>
                <div class="integration-platforms">
                    <span class="platform-badge">Confluence</span>
                    <span class="platform-badge">SharePoint</span>
                    <span class="platform-badge">Notion</span>
                </div>
            </div>
            <div class="integration-footer">
                <button class="btn btn-sm btn-outline" disabled>
                    <i class="bi bi-plus-lg"></i>
                    Connect
                </button>
            </div>
        </div>
        
        <!-- Communication Tools -->
        <div class="integration-card">
            <div class="integration-header">
                <div class="integration-logo communication">
                    <i class="bi bi-chat-text"></i>
                </div>
                <div class="integration-status">Coming Soon</div>
            </div>
            <div class="integration-body">
                <h3 class="integration-title">Communication Tools</h3>
                <p class="integration-description">Deploy your chatbots on popular messaging platforms and communication tools.</p>
                <div class="integration-platforms">
                    <span class="platform-badge">Slack</span>
                    <span class="platform-badge">MS Teams</span>
                    <span class="platform-badge">Discord</span>
                </div>
            </div>
            <div class="integration-footer">
                <button class="btn btn-sm btn-outline" disabled>
                    <i class="bi bi-plus-lg"></i>
                    Connect
                </button>
            </div>
        </div>
        
        <!-- Analytics -->
        <div class="integration-card">
            <div class="integration-header">
                <div class="integration-logo analytics">
                    <i class="bi bi-graph-up"></i>
                </div>
                <div class="integration-status">Coming Soon</div>
            </div>
            <div class="integration-body">
                <h3 class="integration-title">Analytics Tools</h3>
                <p class="integration-description">Connect with analytics platforms to track chatbot performance and user engagement.</p>
                <div class="integration-platforms">
                    <span class="platform-badge">Google Analytics</span>
                    <span class="platform-badge">Mixpanel</span>
                    <span class="platform-badge">Amplitude</span>
                </div>
            </div>
            <div class="integration-footer">
                <button class="btn btn-sm btn-outline" disabled>
                    <i class="bi bi-plus-lg"></i>
                    Connect
                </button>
            </div>
        </div>
        
        <!-- Custom Webhook -->
        <div class="integration-card">
            <div class="integration-header">
                <div class="integration-logo webhook">
                    <i class="bi bi-link-45deg"></i>
                </div>
                <div class="integration-status">Coming Soon</div>
            </div>
            <div class="integration-body">
                <h3 class="integration-title">Custom Webhooks</h3>
                <p class="integration-description">Set up custom webhooks to connect your chatbots with any external system or API.</p>
                <div class="integration-platforms">
                    <span class="platform-badge">REST API</span>
                    <span class="platform-badge">GraphQL</span>
                    <span class="platform-badge">Custom Events</span>
                </div>
            </div>
            <div class="integration-footer">
                <button class="btn btn-sm btn-outline" disabled>
                    <i class="bi bi-plus-lg"></i>
                    Connect
                </button>
            </div>
        </div>
    </div>
    
    <!-- Active Integrations Section (Empty State) -->
    <div class="section-header mt-5">
        <h2>Active Integrations</h2>
        <p class="section-description">Manage your connected external services</p>
    </div>
    
    <div class="active-integrations">
        <div class="empty-state">
            <div class="empty-icon">
                <i class="bi bi-box-seam"></i>
            </div>
            <h3>No Active Integrations</h3>
            <p>You don't have any active integrations yet. Connect with external services to enhance your chatbots.</p>
            <p class="contact-info">Contact us at <a href="mailto:support@example.com">support@example.com</a> to request early access to our integrations.</p>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Refresh button animation
            const refreshBtn = document.getElementById('refreshIntegrations');
            if (refreshBtn) {
                refreshBtn.addEventListener('click', function() {
                    const icon = this.querySelector('i');
                    icon.classList.add('rotate-animation');
                    
                    // Show toast notification
                    showToast('info', 'Checking for new integrations...', 'New integrations will be available soon!');
                    
                    // Remove animation class after spinning
                    setTimeout(() => {
                        icon.classList.remove('rotate-animation');
                    }, 1000);
                });
            }
            
            // Toast notification function
            function showToast(type, title, message) {
                // Create toast element
                const toast = document.createElement('div');
                toast.className = `toast-notification ${type}`;
                
                const icon = type === 'success' 
                    ? '<i class="bi bi-check-circle"></i>' 
                    : type === 'error' 
                        ? '<i class="bi bi-exclamation-circle"></i>'
                        : '<i class="bi bi-info-circle"></i>';
                
                toast.innerHTML = `
                    <div class="toast-icon">
                        ${icon}
                    </div>
                    <div class="toast-content">
                        <div class="toast-title">${title}</div>
                        <div class="toast-message">${message}</div>
                    </div>
                    <button class="toast-close">
                        <i class="bi bi-x"></i>
                    </button>
                `;
                
                // Add to document
                document.body.appendChild(toast);
                
                // Show toast
                setTimeout(() => {
                    toast.classList.add('show');
                }, 100);
                
                // Auto-hide toast after 4 seconds
                setTimeout(() => {
                    toast.classList.remove('show');
                    setTimeout(() => {
                        document.body.removeChild(toast);
                    }, 300);
                }, 4000);
                
                // Close button functionality
                const closeBtn = toast.querySelector('.toast-close');
                closeBtn.addEventListener('click', function() {
                    toast.classList.remove('show');
                    setTimeout(() => {
                        document.body.removeChild(toast);
                    }, 300);
                });
            }
        });
    </script>
}

@section Styles {
    <style>
        /* Integration Page Styles */
        .integrations-container {
            width: 100%;
        }
        
        .section-header {
            margin-bottom: 1.5rem;
        }
        
        .section-header h2 {
            font-size: 1.25rem;
            font-weight: 600;
            margin: 0 0 0.5rem;
            color: var(--color-gray-900);
        }
        
        .section-description {
            color: var(--color-gray-500);
            font-size: 0.875rem;
            margin: 0;
        }
        
        /* Integration Grid */
        .integrations-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
            gap: 1.5rem;
        }
        
        /* Integration Card */
        .integration-card {
            background-color: white;
            border-radius: var(--border-radius-lg);
            box-shadow: var(--shadow);
            overflow: hidden;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            display: flex;
            flex-direction: column;
            height: 100%;
        }
        
        .integration-card:hover {
            transform: translateY(-5px);
            box-shadow: var(--shadow-md);
        }
        
        .integration-header {
            padding: 1.5rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 1px solid var(--color-gray-200);
        }
        
        .integration-logo {
            width: 48px;
            height: 48px;
            border-radius: var(--border-radius);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
        }
        
        .integration-logo.crm {
            background-color: rgba(13, 110, 253, 0.1);
            color: var(--color-primary);
        }
        
        .integration-logo.ticketing {
            background-color: rgba(32, 201, 151, 0.1);
            color: var(--color-success);
        }
        
        .integration-logo.knowledge {
            background-color: rgba(255, 193, 7, 0.1);
            color: var(--color-warning);
        }
        
        .integration-logo.communication {
            background-color: rgba(111, 66, 193, 0.1);
            color: #6f42c1;
        }
        
        .integration-logo.analytics {
            background-color: rgba(13, 202, 240, 0.1);
            color: var(--color-info);
        }
        
        .integration-logo.webhook {
            background-color: rgba(108, 117, 125, 0.1);
            color: var(--color-secondary);
        }
        
        .integration-status {
            font-size: 0.75rem;
            font-weight: 500;
            padding: 0.25rem 0.75rem;
            border-radius: 100px;
            background-color: rgba(255, 193, 7, 0.1);
            color: var(--color-warning);
        }
        
        .integration-body {
            padding: 1.5rem;
            flex: 1;
            display: flex;
            flex-direction: column;
        }
        
        .integration-title {
            font-size: 1.125rem;
            font-weight: 600;
            margin: 0 0 0.75rem;
            color: var(--color-gray-900);
        }
        
        .integration-description {
            font-size: 0.875rem;
            color: var(--color-gray-600);
            margin: 0 0 1rem;
            line-height: 1.5;
        }
        
        .integration-platforms {
            display: flex;
            flex-wrap: wrap;
            gap: 0.5rem;
            margin-top: auto;
        }
        
        .platform-badge {
            font-size: 0.75rem;
            padding: 0.25rem 0.5rem;
            border-radius: var(--border-radius-full);
            background-color: var(--color-gray-100);
            color: var(--color-gray-700);
        }
        
        .integration-footer {
            padding: 1rem 1.5rem;
            border-top: 1px solid var(--color-gray-200);
        }
        
        /* Empty State */
        .active-integrations {
            background-color: white;
            border-radius: var(--border-radius-lg);
            box-shadow: var(--shadow);
            overflow: hidden;
        }
        
        .empty-state {
            padding: 3rem;
            text-align: center;
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        
        .empty-icon {
            font-size: 3rem;
            color: var(--color-gray-300);
            margin-bottom: 1.5rem;
        }
        
        .empty-state h3 {
            font-size: 1.25rem;
            font-weight: 600;
            margin: 0 0 0.75rem;
            color: var(--color-gray-800);
        }
        
        .empty-state p {
            color: var(--color-gray-500);
            max-width: 500px;
            margin: 0 auto 0.5rem;
        }
        
        .contact-info {
            margin-top: 1rem;
            font-size: 0.875rem;
        }
        
        /* Toast Notification */
        .toast-notification {
            position: fixed;
            bottom: 24px;
            right: 24px;
            display: flex;
            align-items: flex-start;
            background-color: white;
            border-radius: var(--border-radius);
            box-shadow: var(--shadow-lg);
            padding: 1rem;
            width: 320px;
            z-index: 1000;
            transform: translateY(100%);
            opacity: 0;
            transition: all 0.3s ease;
        }
        
        .toast-notification.show {
            transform: translateY(0);
            opacity: 1;
        }
        
        .toast-notification.success {
            border-left: 4px solid var(--color-success);
        }
        
        .toast-notification.error {
            border-left: 4px solid var(--color-danger);
        }
        
        .toast-notification.info {
            border-left: 4px solid var(--color-primary);
        }
        
        .toast-icon {
            margin-right: 0.75rem;
            font-size: 1.25rem;
        }
        
        .toast-notification.success .toast-icon {
            color: var(--color-success);
        }
        
        .toast-notification.error .toast-icon {
            color: var(--color-danger);
        }
        
        .toast-notification.info .toast-icon {
            color: var(--color-primary);
        }
        
        .toast-content {
            flex: 1;
        }
        
        .toast-title {
            font-weight: 600;
            margin-bottom: 0.25rem;
        }
        
        .toast-message {
            font-size: 0.875rem;
            color: var(--color-gray-600);
        }
        
        .toast-close {
            background: none;
            border: none;
            font-size: 1.25rem;
            color: var(--color-gray-500);
            cursor: pointer;
            padding: 0;
            margin-left: 0.75rem;
        }
        
        .toast-close:hover {
            color: var(--color-gray-700);
        }
        
        /* Animations */
        @@keyframes rotate {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }
        
        .rotate-animation {
            animation: rotate 1s linear;
        }
        
        /* Utilities */
        .mt-5 {
            margin-top: 3rem;
        }
        
        /* Responsive Adjustments */
        @@media (max-width: 768px) {
            .integrations-grid {
                grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            }
        }
        
        @@media (max-width: 576px) {
            .integrations-grid {
                grid-template-columns: 1fr;
            }
            
            .empty-state {
                padding: 2rem 1rem;
            }
        }
    </style>
}